#include<stdio.h>
int main()
{
    int a,b,c; //int  is a datatype and keyword also
    printf("enter the two number");
    scanf("%d%d",&a,&b); //printf,scanf,main are identifiers
    c=a*b; 
    printf("the multiplication is %d",c);  //semicolon is the punctuators
    return 0; //%d,a,b,c are the constants
}

