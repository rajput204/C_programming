#include<stdio.h>
// #define pi 3.14
int main()
{
    float r,area;
    printf("enter the radius of circle");
    scanf("%f",&r);
    area= 3.14*r*r;
    printf("area of circle is %f ",area);
    return 0;
}