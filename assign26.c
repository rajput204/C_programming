// write a programm to input a number from the user and also input a digit.append a digit in the number and print the resulting number(example:-number=234 and digit=9 now it becomes =2349)
#include<stdio.h>
int main()
{
    int x,y;
    printf("enter the number of digit u want\n");
    scanf("%d",&x);
    printf("enter the single digit ");
    scanf("%d",&y);
    
}